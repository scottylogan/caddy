Simple docker image for [caddy](https://caddyserver.com/), using
Alpine Linux.

Run with a volume containing a Caddyfile mounted on /etc/caddy,
and another volume mounted on /var/log/caddy for logs.

